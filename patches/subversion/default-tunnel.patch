diff --git a/CMakeLists.txt b/CMakeLists.txt
index e950c058f7..b407dbea49 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -96,6 +96,7 @@ option(SVN_ENABLE_AUTH_KWALLET "Enable KWallet auth library" OFF)
 option(SVN_ENABLE_AUTH_GNOME_KEYRING "Enable GNOME Keyring for auth credentials" OFF)
 option(SVN_ENABLE_SASL "Enable Cyrus SASL v2" OFF)
 option(SVN_ENABLE_LIBMAGIC "Enable libmagic filetype detection library")
+cmake_dependent_option(SVN_ENABLE_AUTH_KEYCHAIN "Enable macOS Keychain for auth credentials" ON "APPLE" OFF)
 cmake_dependent_option(SVN_ENABLE_AUTH_GPG_AGENT "Enable GPG Agent support" OFF "WIN32" ON)
 
 option(SVN_INSTALL_PRIVATE_H "Install private header files." OFF)
@@ -720,6 +721,28 @@ if(SVN_ENABLE_AUTH_GNOME_KEYRING)
   )
 endif()
 
+### macOS Keychain
+
+if(SVN_ENABLE_AUTH_KEYCHAIN)
+  add_library(external-keychain INTERFACE)
+  target_link_libraries(external-keychain INTERFACE
+    "-framework CoreFoundation"
+    "-framework Security"
+  )
+
+  add_private_config_definition(
+    "Defined if macOS KeyChain Services support is enabled"
+    "SVN_HAVE_KEYCHAIN_SERVICES" "1"
+  )
+  add_private_config_definition(
+    "Defined if macOS SecItem API is available"
+    "SVN_HAVE_KEYCHAIN_SECITEM_API" "1"
+  )
+
+  target_link_libraries(libsvn_subr PRIVATE external-keychain)
+endif()
+
+
 if (SVN_ENABLE_TUI)
   add_library(external-ncurses INTERFACE)
 
@@ -1111,6 +1134,13 @@ endif()
 string(TOLOWER "${CMAKE_HOST_SYSTEM_PROCESSOR}-${CMAKE_HOST_SYSTEM_NAME}" SVN_BUILD_HOST)
 string(TOLOWER "${CMAKE_SYSTEM_PROCESSOR}-${CMAKE_SYSTEM_NAME}" SVN_BUILD_TARGET)
 
+# Build environment info (hardcoded at configure time)
+set(SVN_BUILD_COMPILER "${CMAKE_C_COMPILER_ID}")
+set(SVN_BUILD_COMPILER_VERSION "${CMAKE_C_COMPILER_VERSION}")
+set(SVN_BUILD_OS "${CMAKE_SYSTEM_NAME}")
+set(SVN_BUILD_OS_VERSION "${CMAKE_SYSTEM_VERSION}")
+set(SVN_BUILD_ARCH "${CMAKE_SYSTEM_PROCESSOR}")
+
 configure_file(
   "${CMAKE_CURRENT_SOURCE_DIR}/subversion/svn_private_config.hc"
   "${CMAKE_CURRENT_BINARY_DIR}/svn_private_config.h"
@@ -1138,6 +1168,7 @@ message(STATUS "    Enable KWallet integration .... : ${SVN_ENABLE_AUTH_KWALLET}
 message(STATUS "    Enable Gnome Keyring .......... : ${SVN_ENABLE_AUTH_GNOME_KEYRING}")
 message(STATUS "    Enable GPG Agent support ...... : ${SVN_ENABLE_AUTH_GPG_AGENT}")
 message(STATUS "    Enable Cyrus SASL v2 .......... : ${SVN_ENABLE_SASL}")
+message(STATUS "    Enable macOS Keychain ......... : ${SVN_ENABLE_AUTH_KEYCHAIN}")
 message(STATUS "  Optionsal features:")
 message(STATUS "    Enable libmagic ............... : ${SVN_ENABLE_LIBMAGIC}")
 message(STATUS "  Optional modules and targets:")
diff --git a/subversion/libsvn_ra_svn/client.c b/subversion/libsvn_ra_svn/client.c
index 8f90dc6493..5991ac9d0b 100644
--- a/subversion/libsvn_ra_svn/client.c
+++ b/subversion/libsvn_ra_svn/client.c
@@ -690,15 +690,47 @@ static svn_error_t *open_session(svn_ra_svn__session_baton_t **sess_p,
     }
   else
     {
+      int port = uri->port ? uri->port : SVN_RA_SVN_PORT;
+
       sess->realm_prefix = apr_psprintf(pool, "<svn://%s:%d>", uri->hostname,
-                                        uri->port ? uri->port : SVN_RA_SVN_PORT);
-
-      SVN_ERR(make_connection(uri->hostname,
-                              uri->port ? uri->port : SVN_RA_SVN_PORT,
-                              &sock, pool));
-      conn = svn_ra_svn_create_conn5(sock, NULL, NULL,
-                                     SVN_DELTA_COMPRESSION_LEVEL_DEFAULT,
-                                     0, 0, 0, 0, pool);
+                                        port);
+
+      /* If the caller installed a tunnel callback, let it handle the
+         plain svn:// connection too (e.g. to forward/proxy it).  The
+         callback will receive tunnel_name == NULL for this case. */
+      if (callbacks
+        && callbacks->check_tunnel_func
+        && callbacks->check_tunnel_func(callbacks->tunnel_baton, NULL)
+        && callbacks->open_tunnel_func)
+        {
+          struct tunnel_data_t *const td = apr_palloc(pool, sizeof(*td));
+
+          td->tunnel_baton = callbacks->tunnel_baton;
+          td->close_tunnel = NULL;
+
+          SVN_ERR(callbacks->open_tunnel_func(
+                      &td->request, &td->response,
+                      &td->close_tunnel, &td->tunnel_context,
+                      callbacks->tunnel_baton, NULL,
+                      uri->user, uri->hostname, port,
+                      callbacks->cancel_func, callbacks_baton,
+                      pool));
+
+          apr_pool_cleanup_register(pool, td, close_tunnel_cleanup,
+                                    apr_pool_cleanup_null);
+
+          conn = svn_ra_svn_create_conn5(NULL, td->response, td->request,
+                                         SVN_DELTA_COMPRESSION_LEVEL_DEFAULT,
+                                         0, 0, 0, 0, pool);
+          SVN_ERR(svn_ra_svn__skip_leading_garbage(conn, pool));
+        }
+      else
+        {
+          SVN_ERR(make_connection(uri->hostname, port, &sock, pool));
+          conn = svn_ra_svn_create_conn5(sock, NULL, NULL,
+                                         SVN_DELTA_COMPRESSION_LEVEL_DEFAULT,
+                                         0, 0, 0, 0, pool);
+        }
     }
 
   /* Build the useragent string, querying the client for any
diff --git a/subversion/svn/help-cmd.c b/subversion/svn/help-cmd.c
index e26a047ec3..98a0c16abb 100644
--- a/subversion/svn/help-cmd.c
+++ b/subversion/svn/help-cmd.c
@@ -154,6 +154,30 @@ svn_cl__help(apr_getopt_t *os,
       opt_state = cmd_baton->opt_state;
     }
 
+  /*
+   * Show build environment info (hardcoded at compile time).
+   */
+  svn_stringbuf_appendcstr(version_footer,
+                           _("Build environment:\n\n"));
+  svn_stringbuf_appendcstr(
+      version_footer,
+      apr_psprintf(pool, _("* Compiler: %s %s\n"),
+                   SVN_BUILD_COMPILER, SVN_BUILD_COMPILER_VERSION));
+  svn_stringbuf_appendcstr(
+      version_footer,
+      apr_psprintf(pool, _("* OS: %s %s\n"),
+                   SVN_BUILD_OS, SVN_BUILD_OS_VERSION));
+  svn_stringbuf_appendcstr(
+      version_footer,
+      apr_psprintf(pool, _("* Architecture: %s\n"), SVN_BUILD_ARCH));
+#ifdef __GLIBC__
+  svn_stringbuf_appendcstr(
+      version_footer,
+      apr_psprintf(pool, _("* libc: glibc %d.%d\n"),
+                   __GLIBC__, __GLIBC_MINOR__));
+#endif
+  svn_stringbuf_appendcstr(version_footer, "\n");
+
   /*
    * Show supported working copy versions.
    */
diff --git a/subversion/svn_private_config.hc b/subversion/svn_private_config.hc
index 1d124de893..5970b028fa 100644
--- a/subversion/svn_private_config.hc
+++ b/subversion/svn_private_config.hc
@@ -33,6 +33,13 @@
 #define SVN_BUILD_HOST "@SVN_BUILD_HOST@"
 #define SVN_BUILD_TARGET "@SVN_BUILD_TARGET@"
 
+/* Build environment info (hardcoded at configure time) */
+#define SVN_BUILD_COMPILER "@SVN_BUILD_COMPILER@"
+#define SVN_BUILD_COMPILER_VERSION "@SVN_BUILD_COMPILER_VERSION@"
+#define SVN_BUILD_OS "@SVN_BUILD_OS@"
+#define SVN_BUILD_OS_VERSION "@SVN_BUILD_OS_VERSION@"
+#define SVN_BUILD_ARCH "@SVN_BUILD_ARCH@"
+
 /* The minimal version of Berkeley DB we want */
 #define SVN_FS_WANT_DB_MAJOR    4
 #define SVN_FS_WANT_DB_MINOR    0
